use lib::common::byte::Byte;

fn main() {
    let alert = Byte::new(["b1", "b2", "b3", "b4", "b5", "b6", "b7", "b8"]);
    println!("{:?}", alert);

    // let num = 10;
    // println!("Hello, world! {} plus one is {}!", num, common::common(num));
}
